/*Faça um programa que determine o mostre os cinco primeiros multiplos de 3, considerando
numeros maiores que 0.*/
#include <stdio.h>
int main (){
    int numero = 0;
    int multiplo = 1;
    int result;
    while (multiplo <= 5){
        result=multiplo * 3;
        printf ("%i\n", result);
        multiplo+=1;
    }printf ("fim do programa ");
}
