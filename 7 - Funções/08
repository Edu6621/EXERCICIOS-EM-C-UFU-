/*8. Sejam a e b os catetos de um triangulo, onde a hipotenusa e obtida pela equacao
hipotenusa = √a^2 + b^2. Faca uma func¸ao que receba os valores de  a e b e calcule o
valor da hipotenusa atraves da equacao. */
#include <stdio.h>
#include <math.h>
void calculo (float a, float b){
    float h;
    h = sqrt((a*a) + (b*b));
    printf ("o valor da hipotenusa é : %.2f", h);
}
int main () {
    float a, b;
    printf ("informe o valor de a : ");
    scanf ("%f", &a);
    printf ("informe o valor de b : ");
    scanf ("%f", &b);
    calculo(a, b);
}
