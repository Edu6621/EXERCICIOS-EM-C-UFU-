/*  Faca um programa para ler as dimensoes de um terreno (comprimento c e largura l),
    bem como o preco do metro de tela p. Imprima o custo para cercar este mesmo terreno
    com tela.          */
#include <stdio.h>
void main () {
    float c, l, p, custo;
    printf ("insira o comprimento do terreno : ");
    scanf ("%f", &c);
    printf ("\ninsira a largura do terreno : ");
    scanf ("%f", &l);
    printf ("\ninsira o preço do metro de tela : ");
    scanf ("%f", &p);
    custo=c*l*p;
    printf ("\n o custo para cercar este terreno é de : %.2f", custo);
}
